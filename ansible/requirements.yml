---
# ansible/requirements.yml
# External dependencies required for security baselines and host management
collections:
  - name: community.general
    version: ">=9.0.0"
  - name: community.docker
    version: ">=3.10.0"
  - name: ansible.posix
    version: ">=2.1.0"
