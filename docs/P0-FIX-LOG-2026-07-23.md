# P0 Fix Log — hint-wallet endpoint (fix/p0-hint-wallet-endpoint)

## 2026-07-23 — Investigation, BLOCKED on missing shared secret

### Summary

Task: implement the Engine-side receiver for the hint-wallet economy
introduced by `CEI-Labs-Wargames@742208a`. Investigation is complete;
implementation is **blocked** on a missing prerequisite that only a human
operator can supply. No application code has been written yet — this is a
documentation-only commit recording the findings so implementation can
resume immediately once unblocked.

### What was read

- `CEI-Labs-Wargames/docs/hint-wallet-sync-deployment.md` (read-only, other
  repo) — the deployment/rotation contract for the "signed Engine sync".
- `CEI-Labs-Wargames/scripts/hint_economy.py` (read-only, other repo) — pure
  cost-derivation logic (progressive 3-tier hint costs as a fraction of
  challenge value, reserve/idempotence math). No transport/signing code
  lives here; that's in `deploy.sh`.
- `CEI-Labs-Wargames/deploy.sh` (read-only, other repo), function
  `sync_hint_wallet_bundle()` (~line 174) — this is where the actual signed
  payload is built and sent. Confirms the contract below.
- This repo's `docker/orchestrator/app/` — existing Flask/gunicorn app.
  `store.py` is SQLite-backed (already solved the multi-worker /
  cross-process consistency problem that an in-memory dict would reintroduce
  — see its module docstring), with `InstanceStore`/`RangeStore` following a
  `reserve()`-before-Docker-call pattern. A `WalletStore` (or `wallet`
  table added to the same SQLite file) is the natural, consistent place for
  per-team balances and the accepted-catalog/revision state — no new
  datastore needed.
- Existing Docker secrets on the host (`docker secret ls`): `ctf_key`,
  `ctfd_db_password`, `ctfd_db_root_password`, `ctfd_secret_key`,
  `orchestrator_admin_password`, `plugin_shared_secret`. Confirmed via
  `grep -rn plugin_shared_secret` across both repos that this secret is an
  **unrelated, pre-existing** mechanism: it authenticates the CTFd
  instance-launcher plugin to the orchestrator's instance-lifecycle API
  (`docker/ctfd/plugins/instance-launcher/`), nothing to do with hint
  catalogs or Wargames CI.

### The documented signing contract (Wargames side, already designed)

From `hint-wallet-sync-deployment.md` + `deploy.sh:sync_hint_wallet_bundle()`:

- **Secret**: env var `HINT_WALLET_SYNC_SECRET` on the Wargames deploy job
  side must exactly match a Docker secret mounted at
  `/run/secrets/hint_wallet_sync_secret` on the Engine side. High-entropy,
  >=32 chars enforced by `deploy.sh` (`${#secret} -ge 32`). Provisioned via
  `openssl rand -base64 48 | docker secret create hint_wallet_sync_secret -`.
  This is a **new, dedicated secret** — explicitly distinct from any
  participant/challenge secret and (per my check above) distinct from
  `plugin_shared_secret`.
- **Signature**: HMAC-SHA256 over the raw JSON bundle bytes, hex digest,
  using that secret as the HMAC key (`hmac.new(secret.encode(), raw,
  hashlib.sha256).hexdigest()` in `deploy.sh`).
- **Payload**: one JSON bundle, `{"schema_version": 1, "revision": <int>,
  "manifests": [...]}`, always containing all three tracks (Bandit,
  Krypton, Natas) together — Engine must validate signature, revision,
  schema, all three track counts, tier costs, and cross-track references,
  then commit all three in a single DB transaction. It must never leave a
  one- or two-track catalog active.
- **Revision semantics**: `HINT_WALLET_REVISION` must be a positive integer,
  strictly increasing. Engine accepts a revision higher than the last
  accepted one; accepts an exact retry of the *same* revision only if the
  signed catalog digest is byte-identical (idempotent retry); rejects a
  lower revision outright; rejects a same-revision/different-digest payload
  (`409 revision_digest_conflict`). No rollback via revision — corrections
  ship as a new higher revision.
- **Required fail-closed HTTP responses**: `401 invalid_signature`,
  `400 invalid_schema`, `400 incomplete_tracks`, `409 stale_revision`,
  `409 revision_digest_conflict`, `422 catalog_validation_failed`,
  `503 secret_or_database_unavailable`. On any of these, the previously
  accepted three-track catalog must remain active untouched.
- **Rotation**: Engine must be able to accept old-and-new secret IDs
  simultaneously during a short overlap window (so this isn't a bare
  single-value secret compare — it implies a small keyed-secret-by-ID
  lookup, not just one env var).
- **Transport**: internal HTTPS only, hostname + cert validation enabled,
  no insecure-TLS override. Audit logging must record secret IDs, revision,
  catalog digest, operator, timestamp, result — **never** the secret,
  signature, or payload body.

### Blocking issue

**The shared secret `hint_wallet_sync_secret` does not exist anywhere.**

Checked and confirmed absent:
- `sudo docker secret ls` on the host — no `hint_wallet_sync_secret` (or
  `cei-labs_hint_wallet_sync_secret`) entry. Only the six pre-existing
  secrets listed above.
- No `docker/secrets/hint_wallet_sync_secret*` file in either repo's
  worktree (`wt-hint-wallet` or `CEI-Labs-Wargames`), no `docker/secrets.example/`
  entry, no `stack.yml` secret definition referencing it.
- `HINT_WALLET_SYNC_SECRET` / `HINT_WALLET_REVISION` appear only inside
  `CEI-Labs-Wargames/deploy.sh` as **consumed** env vars (the deploy job
  expects them to already be set by whatever provisions its environment);
  they are not generated, defaulted, or documented as already-provisioned
  anywhere in either repo.
- `plugin_shared_secret` is a real, live secret but is explicitly a
  different mechanism (CTFd-plugin-to-orchestrator instance-launcher auth,
  pre-existing, unrelated to hint catalogs) — reusing it for hint-wallet
  sync would violate the deployment doc's own instruction that this must be
  a *dedicated* signing secret, and would conflate two independent trust
  boundaries (participant-facing instance API vs. release-pipeline catalog
  push).

Per the task instructions: this is exactly the "shared secret doesn't exist
yet" case that requires a second opinion rather than an invented placeholder
scheme. Inventing a temporary secret or hardcoding a shared value would
create a real security gap in a system with a real venue/headcount deadline,
and directly contradicts the deployment doc's explicit secret-provisioning
process (`docker secret create` from a protected CI/deploy-secret source,
never a tracked file, never printed/logged).

### Blocking question for a human

Who provisions `hint_wallet_sync_secret`, and on what side does it originate?
Two structurally different options, both currently valid per the docs:

1. **Engine generates it**, operator runs `openssl rand -base64 48 |
   docker secret create hint_wallet_sync_secret -` on the Swarm manager,
   then hands the value to whoever controls the Wargames CI secret store
   (out-of-band, one-time, protected channel) to set as
   `HINT_WALLET_SYNC_SECRET` in the deploy job.
2. **Wargames/CI generates it** and it needs to be handed to an Engine
   operator to `docker secret create` on this side.

Either is consistent with the deployment doc (it only says the two sides
must match, not which side originates it). I do not have access to the
Wargames CI secret store to check whether a value already exists there
un-synced to Engine, and creating one unilaterally on the Engine side risks
silently diverging from a value CI already has (or was about to receive).

**Recommendation**: an operator with access to both the Swarm manager
(`docker secret create`) and the Wargames CI secret manager should generate
one value, place it in both locations following the doc's exact
provisioning steps, and confirm back here. I can then implement the Engine
side against the real secret ID immediately — the receiving/validation
logic, schema/revision/HMAC verification, and the `WalletStore` persistence
layer are fully spec'd from the docs above and do not depend on knowing the
secret's actual value, only on it existing as
`/run/secrets/hint_wallet_sync_secret` at runtime.

### Status

- No orchestrator/CTFd-plugin code changed yet.
- No tests added yet (nothing to test against a secret that doesn't exist —
  writing HMAC-verification tests against an invented/placeholder secret
  would need to be redone once the real provisioning happens anyway, so
  held off rather than producing throwaway code).
- Branch `fix/p0-hint-wallet-endpoint` at `8308661` plus this log only.
- **Next steps once unblocked**: add `WalletStore` (SQLite, same file as
  existing `store.py`) with a `wallet_catalog` table (revision, digest,
  tracks/tiers/costs) and a `team_balance` table; add a Flask blueprint/route
  (e.g. `POST /wallet/sync` for the signed Engine-sync endpoint, and
  `POST /wallet/deduct` + `GET /wallet/balance` for the CTFd hint-wallet
  plugin hook, both under existing `_authorized`-style secret-comparison
  pattern in `main.py`); implement the fail-closed status codes and
  all-or-nothing three-track transaction exactly as enumerated above; write
  unit tests plus a concurrent-deduction test (matching the rigor of the
  existing `store.py` reservation tests) to prove double-spend is impossible
  under gunicorn's multi-worker model, same as the instance-store fix
  already did for instance creation races.

## 2026-07-23 — Implementation complete

### Unblocked

`hint_wallet_sync_secret` now exists both as a live Docker Swarm secret
(`sudo docker secret ls`) and as
`/opt/cei-labs/cei-labs-engine/docker/secrets/hint_wallet_sync_secret.txt`
(65 bytes, well over the deployment doc's 32-char minimum), following the
same `file:`-sourced convention `docker/stack.yml` already uses for
`plugin_shared_secret` and the other platform secrets. Implementation
proceeded against the plan recorded above with no further blockers.

### What was built

- **`app/config.py`**: `HINT_WALLET_SYNC_SECRET` (from the
  `hint_wallet_sync_secret` Docker secret / env var fallback, matching the
  existing `_read_secret` pattern) and `HINT_WALLET_SYNC_SECRET_PREVIOUS`
  (from an optional `hint_wallet_sync_secret_previous` secret, present only
  during a coordinated rotation window).

- **`app/wallet.py`** (new module): pure validation, no HTTP/DB
  dependencies, so it's directly unit-testable.
  - `validate_bundle(bundle) -> manifests`: full structural/type schema
    check (exact top-level and per-object key sets, types, positive
    integers, non-empty strings) raising `WalletSchemaError`; track
    completeness (exactly bandit+krypton+natas, once each) raising
    `WalletIncompleteTracksError`; then per-manifest tamper check (recomputed
    digest over `{schema_version,track,entries}` must match the manifest's
    claimed `digest`, replicating deploy.sh's signing script exactly) and
    per-entry economic check (tiers numbered 1,2,3; costs strictly
    increasing) raising `WalletValidationError`.
  - `manifest_digest()`, `find_hint_cost()`, `find_hint_content()` — the
    last two look up a specific tier's price/text out of an already-accepted
    catalog's manifest list.

- **`app/store.py`**: new `WalletStore` class (same file/pattern as
  `InstanceStore`/`RangeStore`, sharing `STORE_DB_PATH` — already how the
  three existing stores coexist in one SQLite file).
  - `wallet_catalog` (singleton row: revision, digest, bundle_json,
    secret_id, accepted_at).
  - `team_balance` (owner_id -> integer balance).
  - `wallet_unlocks` (owner_id, track, entry_name, tier, cost, unlocked_at
    — primary key makes a repeated unlock a no-op read instead of a second
    charge).
  - `try_accept_catalog(revision, digest, manifests, secret_id)`: the
    revision/digest state machine from the deployment doc, entirely inside
    one `BEGIN IMMEDIATE` transaction (same race the existing
    `InstanceStore.reserve()` closes, applied here to catalog syncs):
    returns `"accepted"` (no catalog yet, or strictly higher revision),
    `"idempotent"` (same revision, same digest — no-op, no write),
    `"conflict"` (same revision, different digest), or `"stale"` (lower
    revision). A rejected sync performs no write at all, so the previously
    accepted three-track catalog is guaranteed untouched.
  - `unlock_hint(owner_id, track, entry_name, tier, cost)`: also one
    `BEGIN IMMEDIATE` transaction — check-already-unlocked, check-balance,
    and spend all happen atomically, closing the same double-spend race
    class as `InstanceStore.reserve()`.

- **`app/main.py`**:
  - `POST /wallet/sync` — the one route exempted from the standard
    `X-Orchestrator-Auth` check in `before_request` (different trust
    boundary: the Wargames release pipeline, not the CTFd plugin). Verifies
    `X-Hint-Wallet-Signature` as HMAC-SHA256 over the *raw* request bytes
    against every currently-configured secret (current, and previous during
    rotation) using `hmac.compare_digest`; on match, runs schema ->
    track-completeness -> economic validation via `app.wallet`, computes
    `sha256(raw_body)` as the catalog digest, and calls
    `WalletStore.try_accept_catalog`. Maps outcomes to exactly the
    documented codes: `401 invalid_signature`, `400 invalid_schema`,
    `400 incomplete_tracks`, `422 catalog_validation_failed`,
    `409 stale_revision`, `409 revision_digest_conflict`,
    `503 secret_or_database_unavailable` (no secret configured, or a DB
    exception), `200` on accept/idempotent. Audit-logs secret_id, revision,
    digest, result, timestamp — never the secret, signature, or payload body
    (per the deployment doc's explicit instruction).
  - `POST /wallet/deduct`, `GET /wallet/balance/<owner_id>`,
    `POST /wallet/credit` — the CTFd-plugin-facing hooks sketched in the
    "next steps" section above, authenticated the normal
    `X-Orchestrator-Auth` way like `/instances*`. `deduct` is idempotent on
    retry (`already_unlocked` status, no re-charge) and returns
    `402 insufficient_balance` when the team can't afford the tier,
    `404 hint_not_found` for an unknown track/entry/tier, and
    `409 no_active_catalog` if no bundle has ever been synced.

- **`docker/stack.yml`**: added the `hint_wallet_sync_secret` (file-sourced,
  same convention as `plugin_shared_secret`) to the top-level `secrets:`
  block and to the `orchestrator` service's mounted secrets list, so it's
  actually available at `/run/secrets/hint_wallet_sync_secret` in
  production. `hint_wallet_sync_secret_previous` was deliberately *not*
  added — it only gets provisioned (and added to `docker/secrets/` +
  `stack.yml`) at actual rotation time, per the deployment doc's
  "Coordinated rotation" section; `config.py`'s `_read_secret` already
  degrades gracefully to `""` (ignored) when that file doesn't exist.

### Tests

New: `docker/orchestrator/tests/test_wallet.py` (pure `app.wallet` schema/
economic validation — happy path, every schema violation, both track-
completeness cases, tamper-digest and cost-ordering validation) and
`docker/orchestrator/tests/test_wallet_api.py` (HTTP-level: signature auth
including the retired-old-secret and both-old-and-new-during-rotation
cases; every fail-closed status code from the contract; the full revision/
digest state machine including "reject leaves the prior catalog active,
proven by a subsequent deduct still resolving against the old catalog";
deduct/balance/credit happy path and idempotent-retry; and a
multi-`WalletStore`-object concurrency test — same technique as
`test_store_concurrency.py` — proving exactly one of 20 racing `unlock_hint`
calls for the same team/hint spends the balance and the rest see
`already_unlocked` with no double charge).

Full suite: `cd docker/orchestrator && python3 -m pytest tests/ -q` ->
**150 passed** (103 pre-existing + 47 new), 0 regressions.

### Status

Implementation complete on `fix/p0-hint-wallet-endpoint`. Not yet committed
as of writing this section — see the commit immediately following this
file in `git log` for the final commit hash and push result.
