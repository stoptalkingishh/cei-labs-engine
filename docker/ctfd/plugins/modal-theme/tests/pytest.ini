[pytest]
# Pins pytest's rootdir to this tests/ directory, same reasoning as
# instance-launcher/tests/pytest.ini: without this, pytest's package-root
# detection climbs into the parent plugin directory and tries to import
# its __init__.py (which requires CTFd, not installed here).
