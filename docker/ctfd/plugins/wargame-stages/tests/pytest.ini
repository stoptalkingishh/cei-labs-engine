[pytest]
# Pins pytest's rootdir to this tests/ directory. Without this, pytest's
# package-root detection climbs up into the parent plugin directory and tries
# to import its __init__.py (which requires CTFd, not installed here) as
# part of resolving the test module's dotted name.
